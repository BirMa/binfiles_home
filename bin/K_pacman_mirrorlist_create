#! /usr/bin/sh

if [[ ${EUID} -ne 0 ]]; then
  echo 'need to be root'
  exit 1
fi

reflector --country Germany --country Switzerland --country Sweden --country Norway --country Netherlands --protocol https  --fastest 10 | sed '/^$/d' | sed '/^#/d' | sort -R | tee /etc/pacman.d/mirrorlist
